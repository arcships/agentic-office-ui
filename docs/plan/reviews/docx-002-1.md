> ⚠️ **已失效（2026-07-07）**：本 review 针对的是已回退的机械复制代码（editor-helpers.ts 23445 行等）。
> 当前采用模块化重做，见 `docs/docx-migration-architecture.md`。

---

---
task: docx-002
reviewer: dimcode
date: 2026-07-07
---

# Review: docx-002 — editor-helpers 复制+清理

## Findings

### P3 — non-blocking — 任务 steps 中列出的 PointerEvent/MouseEvent 替换实际未触发

- 设计文档：`docs/upstream-docx-feature-alignment.md` Phase 2 步骤 2（L477-478）列出 `React.PointerEvent<HTMLElement>` 和 `React.MouseEvent<HTMLElement>` 的 sed 替换。
- 代码：`packages/docx-core/src/editor-helpers.ts`
- 上游前半部分这两处 React 类型分别出现在 L18732 / L18747，均落在 `renderParagraphRuns`（18126–19581）函数体内。该函数按步骤 3 整体抽出待 vue-docx 重写，因此 `editor-helpers.ts` 中实际不存在这两类标注，`Record<string, unknown>` 替换计数为 0。
- 结论：实际产出与设计一致，仅是 steps 文案未说明“替换计数可能为 0”。无功能影响。

### P3 — non-blocking — React.CSSProperties 替换计数（44）少于上游（52）属正常

- 设计文档：`docs/upstream-docx-feature-alignment.md` 2.5（L243，73 处）与要点 43（L400，25 个函数）。
- 代码：`packages/docx-core/src/editor-helpers.ts`，44 处 `Record<string, string | number | undefined>`。
- 上游前半部分共 52 处 `React.CSSProperties`，其中 8 处位于被抽出的 `renderParagraphRuns` 内（18126–19581），抽除后剩 44 处，与产出精确吻合。`React.ReactNode` 上游 17 处中 11 处在被抽函数内，余下 6 处已替换为 `unknown`。
- 结论：计数差异完全由函数抽出解释，无遗漏。

### P3 — non-blocking — shouldSyncActiveRangeOnKeyUp 类型扩展超出 sed 模板

- 设计文档：`docs/upstream-docx-feature-alignment.md` Phase 2 步骤 2（L476）的 `React.KeyboardEvent<HTMLElement>` 替换模板为 `{ key: string; shiftKey: boolean; ctrlKey: boolean; metaKey: boolean; preventDefault: () => void }`。
- 代码：`packages/docx-core/src/editor-helpers.ts:22672`
- 该函数函数体访问 `event.nativeEvent`，开发者将替换类型扩展为 `{ ...; nativeEvent: KeyboardEvent }`，避免机械 sed 后的类型错误。这是正确的自适应调整，typecheck 通过。
- 结论：合理偏离，建议后续在迁移说明里补一句备注。

### P3 — non-blocking — Buffer ambient 声明为新增内容

- 代码：`packages/docx-core/src/editor-helpers.ts:98-103`
- 为 `toBase64` 的 Node 快路径添加了 `declare const Buffer` 环境声明，避免引入 `@types/node`。上游在 React 工程内有 Node 类型可用，迁移到零依赖的 docx-core 后这是必要的可移植性处理。
- 结论：合理，无阻塞。

## 验证结果

| 检查项 | 结果 |
|---|---|
| `grep "React\." editor-helpers.ts` | 0 命中（零 React 类型残留） |
| `renderParagraphRuns` / `renderStaticHtml` / `renderToStaticMarkup` 残留 | 0 命中（已抽出） |
| 13 个核心纯函数保留 | 全部命中（resolveDualWrappedFloatingImageGeometry、resolveParagraphDualWrappedTextLayout、buildParagraphPretextLayoutSource、estimateParagraphLineHeightPx、paragraphLineCountWithinWidth、estimateTableRowHeightsPx、buildDocumentPageNodeSegments、buildRenderColumnSegmentsForPageSection、buildParagraphNumberingLabels、resolveHeaderPaginationReservePx、resolveMeasuredPageContentHeightPx、resolveDropCapFontSizePx、defaultStarterModel） |
| 4 个样式函数保留 | 全部命中（wrappedFloatingImageStyle、absoluteFloatingImageStyle、runStyleToCss、paragraphBlockStyle） |
| `editor-types.ts` 导出 DocxEditorSelection / DocxTextRange / DocxTextRangeLocation / DocxTextRangeBoundary | 命中 |
| `core/state.ts` 引用 `../editor-types` | 命中（`packages/docx-core/src/core/state.ts:7-9`） |
| `pnpm --filter @extend-ai/docx-core typecheck` | 通过 |
| `pnpm --filter @extend-ai/docx-core build` | 通过（dist/index.d.ts 104.65 KB） |
| `index.ts` 导出 wiring | `editor-types` 与 `editor-helpers` 均 `export *`，并对 `buildDocumentPageNodeSegments` 等 upstream 重复项显式重导出消歧（`packages/docx-core/src/index.ts:86-94`） |

## 结论

pass

任务全部 steps 落地：editor.tsx 1–24953 行提取为 `editor-helpers.ts`（23445 行，差异由函数抽出解释），React 类型标注清理彻底（零 `React.` 残留），两个 JSX 函数已抽出，类型提取到 `editor-types.ts` 并被 `core/state.ts` 正确引用，13 个核心纯函数与 4 个样式函数完整保留，typecheck 与 build 均通过。三条 P3 均为 non-blocking 说明性事项。
