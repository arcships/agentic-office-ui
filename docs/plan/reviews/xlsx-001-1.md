# xlsx-001 Review

## Findings

### P2 — Non-blocking

| # | Finding | Design doc | Code location |
|---|---|---|---|
| 1 | **`src/xlsx-worker.js` 包装文件缺失**。上游 `worker-client.ts` 通过 `new URL("./xlsx-worker.js", import.meta.url)` 创建 Worker，上游有一个 1 行的 `xlsx-worker.js`（`import "./xlsx-worker.ts"`）作为 Vite worker entry 的桥接文件。当前 xlsx-core/src 中未复制此文件。设计文档 Phase 1 明确列出 `cp $UPSTREAM/xlsx-worker.js $CORE/xlsx-worker.js`。消费者（Vite）可直接解析 `.ts` worker entry，但上游约定是通过 `.js` 包装文件。 | `docs/upstream-xlsx-feature-alignment.md` §四 Phase 1 (line 467) | `packages/xlsx-core/src/worker-client.ts:112` |

### P3 — Non-blocking

| # | Finding | Design doc | Code location |
|---|---|---|---|
| 2 | **LICENSE 文件缺失**。上游 `@extend-ai/react-xlsx` 为 MIT license。设计文档 Phase 9 要求将 LICENSE 复制到 `packages/xlsx-core/LICENSE`。当前 package.json 未声明 license 字段，且无 LICENSE 文件。 | `docs/upstream-xlsx-feature-alignment.md` §四 Phase 9 (line 686) | `packages/xlsx-core/` (缺失 LICENSE) |
| 3 | **Worker 入口未被 tsup 构建**。`tsup` 仅构建 `src/index.ts`，不构建 `src/xlsx-worker.ts`。`worker-client.ts` 中 `new URL("./xlsx-worker.js", import.meta.url)` 在 tsup 输出中保留为字面量，运行时需消费者（Vite/demo）提供 worker 打包。这是架构决策（xlsx-core 是引擎层，由 vue-xlsx/demo 层负责 worker 部署），但未在 package.json 或 README 中说明。建议在 package.json 增加 `"exports"` 中声明 worker 入口，或在 tsup 配置中增加 worker entry。 | `docs/upstream-xlsx-feature-alignment.md` §9.2 (line 924: `entry: ["src/index.ts", "src/xlsx-worker.ts"]`) | `packages/xlsx-core/package.json:10` |

## Verification Summary

| 检查项 | 结果 |
|---|---|
| `grep -r "React\." packages/xlsx-core/src/` | PASS — 零匹配 |
| `pnpm --filter @extend-ai/xlsx-core typecheck` | PASS — 零错误 |
| `pnpm --filter @extend-ai/xlsx-core build` | PASS — 构建成功，产出 `dist/index.js` (262KB) + `dist/index.d.ts` (54KB) + `dist/duke_sheets_wasm_bg.wasm` |
| 7 个直接复制文件与上游逐字节相同 | PASS — wasm.ts, safe-calculate.ts, worker-client.ts, xlsx-worker.ts, colors.ts, images.ts, charts.ts 全部 identical |
| React 类型清理（types.ts）| PASS — `React.CSSProperties`→`Record<string, string \| number \| undefined>`, `React.ReactNode`→`unknown`, `React.PointerEvent`→`Record<string, unknown>`, `React.Ref`→`unknown`, `React.HTMLAttributes`/`ButtonHTMLAttributes`→`Record<string, unknown>`, `XlsxViewerProps`/`XlsxViewerProviderProps` 已删除 |
| import 路径 | PASS — 无循环依赖，所有内部引用指向同目录文件 |
| `docs/INDEX.md` 导出状态更新 | PASS — 标记为 `✅ done` |
| package.json 依赖 | PASS — @dukelib/sheets-wasm, fflate, d3-*, regl, topojson-client, us-atlas, world-atlas 均已声明 |
| copy-wasm.mjs 脚本 | PASS — 可从 @dukelib/sheets-wasm 复制 wasm 到 dist/ |

## 结论

**PASS**

3 个 finding 均为 non-blocking（1 P2 + 2 P3）。核心交付物完整：7 个引擎文件逐字节对齐上游，types.ts 的 React 类型全部清理，typecheck 与 build 均通过，跨模块 import 无循环依赖。`xlsx-core` 可作为独立引擎包被 `vue-xlsx` 层引用。
